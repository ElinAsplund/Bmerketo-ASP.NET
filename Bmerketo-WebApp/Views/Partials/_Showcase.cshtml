@*@model ShowcaseModel
*@

@*@inject ShowcaseService showcaseService
*@
@{
    var showcase = showcaseService.GetLatest();
}


@*<!-- showcase start -->
<section class="showcase">
    <div class="container">
        <div class="content">
            <div class="ingress">HARD CODED</div>
            <div class="title">Hard coded</div>
            <a class="btn-theme" asp-controller="products" asp-action="Index">SHOP NOW</a>
        </div>
        <div class="content-image">
            <img src="./images/chair.jpg" alt="golden chair">
        </div>
    </div>
</section>
<!-- showcase end -->*@

<!-- showcase dynamic start -->

@if(showcase != null)
{
    <section class="showcase">
        <div class="container">
            <div class="content">
                <div class="ingress">@showcase?.Ingress</div>
                <div class="title">@showcase?.Title</div>

                @if (!string.IsNullOrEmpty(showcase?.Button?.Url) && !string.IsNullOrEmpty(showcase?.Button?.Content))
                {
                    <a class="btn-theme" href="@showcase?.Button?.Url">@showcase?.Button?.Content</a>
                }

            </div>
            <div class="content-image">
                <img src="@showcase?.ImageUrl" alt="@showcase?.Title">
            </div>
        </div>
    </section>  
}
<!-- showcase dynamic end -->